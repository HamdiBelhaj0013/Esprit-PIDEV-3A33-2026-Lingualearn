<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>
    <meta http-equiv="X-UA-Compatible" content="ie=edge"/>
    <title>{% block title %}Language Learning Platform{% endblock %}</title>

    {# Tabler CSS #}
    <link href="https://cdn.jsdelivr.net/npm/@tabler/core@1.0.0-beta17/dist/css/tabler.min.css" rel="stylesheet"/>
    <link href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@latest/tabler-icons.min.css" rel="stylesheet"/>

    {% block stylesheets %}{% endblock %}
</head>
<body>
<div class="page">
    {# Sidebar #}
    <aside class="navbar navbar-vertical navbar-expand-lg" data-bs-theme="dark">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#sidebar-menu" aria-controls="sidebar-menu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <h1 class="navbar-brand navbar-brand-autodark">
                <a href="{{ path('admin_dashboard') }}">
                    <i class="ti ti-language fs-1"></i>
                    <span class="ms-2">LinguaLearn</span>
                </a>
            </h1>

            <div class="collapse navbar-collapse" id="sidebar-menu">
                <ul class="navbar-nav pt-lg-3">
                    {# Dashboard #}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_dashboard') }}">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-home"></i>
                                </span>
                            <span class="nav-link-title">Dashboard</span>
                        </a>
                    </li>

                    {# User Management #}
                    <li class="nav-item dropdown active">
                        <a class="nav-link dropdown-toggle" href="#navbar-user" data-bs-toggle="dropdown" data-bs-auto-close="false" role="button" aria-expanded="true">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-users"></i>
                                </span>
                            <span class="nav-link-title">User Management</span>
                        </a>
                        <div class="dropdown-menu show">
                            <a class="dropdown-item" href="{{ path('admin_users_index') }}">
                                <i class="ti ti-list me-2"></i> All Users
                            </a>
                            <a class="dropdown-item" href="{{ path('admin_users_new') }}">
                                <i class="ti ti-user-plus me-2"></i> Create User
                            </a>
                        </div>
                    </li>

                    {# Courses (Placeholder for Module 2) #}
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-book"></i>
                                </span>
                            <span class="nav-link-title">Courses</span>
                        </a>
                    </li>

                    {# Exercises (Placeholder for Module 3) #}
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-puzzle"></i>
                                </span>
                            <span class="nav-link-title">Exercises</span>
                        </a>
                    </li>

                    {# International Tests #}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#navbar-tests" data-bs-toggle="dropdown" data-bs-auto-close="false" role="button" aria-expanded="false">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-certificate"></i>
                                </span>
                            <span class="nav-link-title">International Tests</span>
                        </a>
                        <div class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('mocktest_index') }}">
                                <i class="ti ti-file-text me-2"></i> Mock Tests
                            </a>
                            <a class="dropdown-item" href="{{ path('testquestion_index') }}">
                                <i class="ti ti-help me-2"></i> Test Questions
                            </a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('mocktest_new') }}">
                                <i class="ti ti-plus me-2"></i> Create Mock Test
                            </a>
                            <a class="dropdown-item" href="{{ path('testquestion_new') }}">
                                <i class="ti ti-plus me-2"></i> Create Question
                            </a>
                        </div>
                    </li>

                    {# Forum (Placeholder for Module 5) #}
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-messages"></i>
                                </span>
                            <span class="nav-link-title">Forum</span>
                        </a>
                    </li>

                    {# Support (Placeholder for Module 6) #}
                    <li class="nav-item">
                        <a class="nav-link" href="#">
                                <span class="nav-link-icon d-md-none d-lg-inline-block">
                                    <i class="ti ti-lifebuoy"></i>
                                </span>
                            <span class="nav-link-title">Support</span>
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </aside>

    {# Header #}
    <header class="navbar navbar-expand-md d-print-none">
        <div class="container-xl">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar-menu" aria-controls="navbar-menu" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="navbar-nav flex-row order-md-last">
                {# User Menu #}
                {% if app.user %}
                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link d-flex lh-1 text-reset p-0" data-bs-toggle="dropdown" aria-label="Open user menu">
                            <span class="avatar avatar-sm" style="background-image: url(https://ui-avatars.com/api/?name={{ app.user.firstName }}+{{ app.user.lastName }}&background=206bc4&color=fff)"></span>
                            <div class="d-none d-xl-block ps-2">
                                <div>{{ app.user.fullName }}</div>
                                <div class="mt-1 small text-muted">Administrator</div>
                            </div>
                        </a>
                        <div class="dropdown-menu dropdown-menu-end dropdown-menu-arrow">
                            <a href="#" class="dropdown-item">
                                <i class="ti ti-user me-2"></i> Profile
                            </a>
                            <a href="#" class="dropdown-item">
                                <i class="ti ti-settings me-2"></i> Settings
                            </a>
                            <div class="dropdown-divider"></div>
                            <a href="#" class="dropdown-item">
                                <i class="ti ti-logout me-2"></i> Logout
                            </a>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </header>

    {# Page Content #}
    <div class="page-wrapper">
        {# Page header #}
        {% block page_header %}
            <div class="page-header d-print-none">
                <div class="container-xl">
                    <div class="row g-2 align-items-center">
                        <div class="col">
                            <h2 class="page-title">
                                {% block page_title %}Dashboard{% endblock %}
                            </h2>
                            {% block breadcrumb %}{% endblock %}
                        </div>
                        {% block page_actions %}{% endblock %}
                    </div>
                </div>
            </div>
        {% endblock %}

        {# Flash Messages #}
        <div class="container-xl mt-3">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label == 'error' ? 'danger' : label }} alert-dismissible" role="alert">
                        <div class="d-flex">
                            <div>
                                {% if label == 'success' %}
                                    <i class="ti ti-check icon alert-icon"></i>
                                {% elseif label == 'error' or label == 'danger' %}
                                    <i class="ti ti-alert-circle icon alert-icon"></i>
                                {% elseif label == 'warning' %}
                                    <i class="ti ti-alert-triangle icon alert-icon"></i>
                                {% else %}
                                    <i class="ti ti-info-circle icon alert-icon"></i>
                                {% endif %}
                            </div>
                            <div>{{ message }}</div>
                        </div>
                        <a class="btn-close" data-bs-dismiss="alert" aria-label="close"></a>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

        {# Page body #}
        <div class="page-body">
            <div class="container-xl">
                {% block body %}{% endblock %}
            </div>
        </div>

        {# Footer #}
        <footer class="footer footer-transparent d-print-none">
            <div class="container-xl">
                <div class="row text-center align-items-center flex-row-reverse">
                    <div class="col-lg-auto ms-lg-auto">
                        <ul class="list-inline list-inline-dots mb-0">
                            <li class="list-inline-item">
                                <a href="#" class="link-secondary">Documentation</a>
                            </li>
                        </ul>
                    </div>
                    <div class="col-12 col-lg-auto mt-3 mt-lg-0">
                        <ul class="list-inline list-inline-dots mb-0">
                            <li class="list-inline-item">
                                Copyright &copy; {{ 'now'|date('Y') }}
                                <a href="#" class="link-secondary">LinguaLearn Platform</a>.
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </footer>
    </div>
</div>

{# Tabler JS #}
<script src="https://cdn.jsdelivr.net/npm/@tabler/core@1.0.0-beta17/dist/js/tabler.min.js"></script>

{% block javascripts %}{% endblock %}

{% include 'components/_delete_modal.html.twig' %}
</body>
</html>
